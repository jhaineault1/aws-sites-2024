name: Deploy Infrastructure

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Deploy static site to S3 bucket
        run: aws s3 sync ./arlo-co.com s3://www.arlo-co.com --delete

      - name: Deploy static site to S3 bucket
        run: aws s3 sync ./arlo-co.com s3://arlo-co.com --delete

  # Invalidate_CloudFront:
  #   name: Invalidate CloudFront Distribution
  #   needs: Sync_S3
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Invalidate CloudFront
  #       uses: chetan/invalidate-cloudfront-action@v2

  #       env:
  #         DISTRIBUTION: ${{ secrets.DISTRIBUTION }}
  #         PATHS: "/*"
  #         AWS_REGION: "us-east-1"
  #         AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  # - name: Deploy static site to S3 bucket
  #   run: aws s3 sync ./your-sturdy-home/static s3://www.yoursturdyhome.com --delete

  # - name: Deploy static site to S3 bucket
  #   run: aws s3 sync ./jh-portfolio/static s3://www.jh-portfolio.com --delete

  # - name: Terraform Init
  #   uses: hashicorp/terraform-github-actions/init@v0.4.0
  #   env:
  #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #     TF_ACTION_WORKING_DIR: 'terraform'
  #     AWS_ACCESS_KEY_ID:  ${{ secrets.AWS_ACCESS_KEY_ID }}
  #     AWS_SECRET_ACCESS_KEY:  ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  # - name: Terraform Validate
  #   uses: hashicorp/terraform-github-actions/validate@v0.3.7

  # - name: Terraform Apply
  #   uses: hashicorp/terraform-github-actions/apply@v0.4.0
  #   env:
  #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #     TF_ACTION_WORKING_DIR: 'terraform'
  #     AWS_ACCESS_KEY_ID:  ${{ secrets.AWS_ACCESS_KEY_ID }}
  #     AWS_SECRET_ACCESS_KEY:  ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  # - name: Sync S3
  #   uses: jhaineault1/s3-sync-action@master
  #   env:
  #     SOURCE_DIR: '../src'
  #     AWS_REGION: 'us-east-1'
  #     AWS_S3_BUCKET: 'your-sturdy-home'
  #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
